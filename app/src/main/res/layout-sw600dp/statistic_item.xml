<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="118dp"
    android:layout_height="90dp"
    android:paddingBottom="2dp" >

    <RelativeLayout
        android:id="@+id/rlStatisticItem"
        android:background="@android:color/white"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <RelativeLayout
            android:id="@+id/rlStatisticIcon"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_alignParentTop="true"
            android:layout_centerInParent="true"
            android:layout_marginTop="5dp"
            android:gravity="center" >

            <ee.app.conversabusiness.view.CircleImageView
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:id="@+id/ivStatisticImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_centerInParent="true"
                android:contentDescription="@null"
                android:elevation="1dp"
                app:border_color="@android:color/transparent"/>
        </RelativeLayout>

        <TextView
            android:id="@+id/tvStatisticInfo"
            android:layout_below="@+id/rlStatisticIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:layout_centerHorizontal="true"
            android:text="fasdasd"
            android:textColor="@color/light_gray_subtext"
            android:textSize="15sp" />
    </RelativeLayout>
</RelativeLayout>